const request = require('supertest');
const app = require('../../../app'); // reference to you app.js file

const prepare = require('../../../services/current/prepare');

describe('/api/current', () => {
  it('TODO', () => {
    // TODO write "THE" Katests
  });
});
